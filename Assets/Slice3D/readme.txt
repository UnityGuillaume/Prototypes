9-slice applied to mesh.
Define a box that intersect a mesh, then use the scale value (on the script, not
the transform) to scale it. Everything outside the box stay the same scale,
everything isnide get stretched
