using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UIStyleContainer : MonoBehaviour
{
    //match a property name to an override value (or "null" if no override);
    public Dictionary<string, string> propertiesLinks;
}
